---
- name: apply/update the Scripts role
  hosts:
    - ubuntu_debian_hosts:!apt_cache:!influxDB_vm:!grafana_vm:!seedbox
    - tesseract
    - event_horizon
    - dilithium
  roles:
    - git_scripts_role
  tasks:

  #TODO: handle seedbox
  