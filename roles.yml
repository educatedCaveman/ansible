---
- linux_role:
  - install certain packages
  - dotfiles
    - zsh
    - netdata
    - nvim
  - home directory permissions
  - MOTD
  
- docker role:
  - configure the repo, etc.

- apt_client role:
  - apt_cache config

- VM role:
  - install qemu agent

- LXC role:
  - install git

- drake_role:
  - set permissions on home directory