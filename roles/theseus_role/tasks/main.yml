---
# packages
- name: install packages
  import_tasks: packages.yml

# GitHub
- name: clone repos
  import_tasks: github.yml

# ZSH
- name: configure ZSH
  import_tasks: ZSH.yml
 
# NeoVim
- name: configure NeoVim & Vundle
  import_tasks: neovim.yml  

# TMUX
- name: configure TMUX
  import_tasks: tmux.yml

# Discord (configfile)
- name: install Discord config
  import_tasks: discord.yml

# NFS
- name: mount NFS shares
  import_tasks: NFS.yml

# UDEV Rules
- name: install UDEV rules
  import_tasks: udev.yml

# Dynamic Wallpaper
- name: dynamic wallpaper files
  import_tasks: wallpaper.yml

# Services
- name: Start services
  import_tasks: services.yml

# disks
- name: Mount extra disk
  mount:
    path:   /data
    src:    UUID=cc0b8e10-36e4-4d7e-bcb5-4591f65499cc
    fstype: btrfs
    # opts:
    state:  mounted

# TODO: pull KDE UI and preference configs

# TODO: syncthing/other data

# final installs:
- name: Manual software installs
  debug:
    msg: 
      - "run the following command(s):"
      - "sudo pamac install ultra-flat-icons-blue brother-hll2395dw vscodium superslicer-bin superslicer-profiles-git openlens-bin jellyfin-media-player plasma5-wallpapers-dynamic"
      - "flatpak install flathub tv.plex.PlexDesktop"
      - "flatpak install flathub com.plexamp.Plexamp"
