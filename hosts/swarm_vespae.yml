# configure Docker Swarm Cluster (DEV)
- name: configure Docker Swarm Cluster (DEV)
  hosts:
    - dev_swarm
  roles: 
    - git_config_role
    - git_dotfiles_role
    - linux_role
    - drake_role
    - docker_role
    - nfs_base_role
  tasks: